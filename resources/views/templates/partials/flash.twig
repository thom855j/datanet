{% if flash.getMessage('info') %}
    <div class="form-signin text-center alert alert-info">
        {{ flash.getMessage('info') | first }}
    </div>
{% endif %}

{% if flash.getMessage('error') %}
    <div class="form-signin text-center alert alert-danger">
        {{ flash.getMessage('error') | first }}
    </div>
{% endif %}